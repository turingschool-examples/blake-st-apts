<h1>Owners</h1>

<ul id="owners">
  <% @owners.each do |owner| %>
    <li><%= owner.first_name %> <%= owner.last_name %>
      <ul>
        <% owner.units.each do |unit| %>
          <li><%= unit.name %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
